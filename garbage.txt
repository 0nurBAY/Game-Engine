if(glfwGetKey(window.GetWindow(), GLFW_KEY_UP) == GLFW_PRESS) {
            npc_y+=500.0f * deltatime * std::cos(glm::radians(transformcomponent->rotation));
            npc_x-=500.0f * deltatime * std::sin(glm::radians(transformcomponent->rotation));
            std::cout << "NPC Y:" << npc_y <<"\n";
        }
        if(glfwGetKey(window.GetWindow(), GLFW_KEY_LEFT) == GLFW_PRESS) {
            // npc_x-=1.0f * deltatime;
            // std::cout << "NPC X:" << npc_x <<"\n";
            transformcomponent->rotation += 100.0f* deltatime;
            std::cout << "NPC Angle:" << transformcomponent->rotation <<"\n";
        }
        if(glfwGetKey(window.GetWindow(), GLFW_KEY_DOWN) == GLFW_PRESS) {
            npc_y-=500.0f * deltatime * std::cos(glm::radians(transformcomponent->rotation));
            npc_x+=500.0f * deltatime * std::sin(glm::radians(transformcomponent->rotation));
            std::cout << "NPC Y:" << npc_y <<"\n";
        }
        if(glfwGetKey(window.GetWindow(), GLFW_KEY_RIGHT) == GLFW_PRESS) {
            // npc_x+=1.0f * deltatime;
            // std::cout << "NPC X:" << npc_x <<"\n";
            transformcomponent->rotation -= 100.0f * deltatime;
            std::cout << "NPC Angle:" << transformcomponent->rotation <<"\n";
        }